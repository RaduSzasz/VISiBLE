import { Component } from '@angular/core';

@Component({
  selector: 'my-app',
  styleUrls: ['src/app/app.component.css'],
  template: `
    <h1>{{title}}</h1>
    `
})
export class AppComponent {
  title = 'VISiBLE';
}
